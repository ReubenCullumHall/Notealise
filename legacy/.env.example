# Copy to legacy/.env and point at a real folder of .md files.
# Restart `npm run dev:legacy` after changing this — Vite only reads it at server startup.
# Leave unset (or delete the file) to fall back to this browser's localStorage vault.
VAULT_DIR=/absolute/path/to/a/notes/folder
